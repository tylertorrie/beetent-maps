// Copy this file to  firebase-config.js  (gitignored) and paste your Firebase
// web app config. See FIREBASE_SETUP.md. The web config is not secret — security
// is enforced by the database rules — but we keep it out of the public repo so
// crews can't be impersonated trivially.
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR-PROJECT.firebaseapp.com",
  databaseURL: "https://YOUR-PROJECT-default-rtdb.firebaseio.com",
  projectId: "YOUR-PROJECT",
};
